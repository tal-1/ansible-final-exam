# Points about Ansible

1) the pros of ansible over AMI are: readability, compatibility with different OSs, editing the playbook instead of building a new image.
2) the cons of ansible over AMI are: speed, dependent on repositories and packages, the AMI is ready to run.
